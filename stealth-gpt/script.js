(()=>{
'use strict';
const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const clamp=(n,a=0,b=1)=>Math.max(a,Math.min(b,n));
const range=(p,a,b)=>clamp((p-a)/(b-a));
const ease=t=>{t=clamp(t);return t<.5?4*t*t*t:1-Math.pow(-2*t+2,3)/2};
const mix=(a,b,t)=>a+(b-a)*t;
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
const isMobile=()=>innerWidth<=900;
const prog=el=>{const r=el.getBoundingClientRect();const travel=Math.max(1,el.offsetHeight-innerHeight);return clamp(-r.top/travel)};
const loaderBar=$('.loader i em'), loaderPct=$('.loader small');
let lp=0; const loading=setInterval(()=>{lp=Math.min(92,lp+Math.ceil(Math.random()*8));if(loaderBar)loaderBar.style.width=lp+'%';if(loaderPct)loaderPct.textContent=lp+'%';},70);
addEventListener('load',()=>{clearInterval(loading);if(loaderBar)loaderBar.style.width='100%';if(loaderPct)loaderPct.textContent='100%';setTimeout(()=>document.documentElement.classList.add('ready'),180)},{once:true});
setTimeout(()=>document.documentElement.classList.add('ready'),1500);
if(!reduced && window.Lenis){try{const lenis=new Lenis({duration:1.05,smoothWheel:true,wheelMultiplier:.9,touchMultiplier:1.05});const raf=t=>{lenis.raf(t);requestAnimationFrame(raf)};requestAnimationFrame(raf)}catch(e){}}
const head=$('.head'), bar=$('.progress i');
const hero=$('.hero'), copyA=$('.copy-a'), copyB=$('.copy-b'), works=$$('[data-work]');
const cases=$('.cases'), track=$('#caseTrack'), caseEls=$$('[data-case]'), caseFill=$('#caseFill'), caseNum=$('#caseNum');
const pivot=$('.pivot'), sels=$$('.sel'), selCore=$('.sel-core');
const build=$('.build'), inputs=$$('.input'), product=$('.product'), output=$('.output');
const evolve=$('.evolve'), newEls=$$('.ev-app .new'), need=$('.need');
const heroDesktop=[{x:-15,y:8,r:-4,s:.74},{x:14,y:-1,r:3,s:.72},{x:-5,y:-8,r:-2,s:.74},{x:13,y:2,r:5,s:.70},{x:-10,y:-6,r:-3,s:.68},{x:10,y:4,r:3,s:.74},{x:-4,y:11,r:-2,s:.72},{x:8,y:-8,r:2,s:.70},{x:-9,y:4,r:-4,s:.72}];
const heroMobile=[{x:-18,y:-3,r:-3,s:.78},{x:16,y:-5,r:3,s:.76},{x:-12,y:4,r:-2,s:.77},{x:15,y:-4,r:4,s:.75},{x:-10,y:5,r:-2,s:.72},{x:12,y:-6,r:2,s:.77},{x:-8,y:6,r:-2,s:.74},{x:8,y:-7,r:2,s:.73},{x:-6,y:4,r:-3,s:.75}];
function heroMotion(){if(!hero||reduced)return;const p=prog(hero),m=isMobile(),accumulate=ease(range(p,.03,.34)),gather=ease(range(p,.42,.78)),fade=ease(range(p,.77,.96)),vectors=m?heroMobile:heroDesktop;works.forEach((el,i)=>{const v=vectors[i%vectors.length],arrive=Math.max(0,1-ease(range(p,Math.max(.02,i*.025),Math.min(.38,.12+i*.035)))),initialX=(i%2?1:-1)*(m?22:12)*arrive,initialY=(i%3-1)*(m?7:4)*arrive,chaos=(i%2?1:-1)*accumulate*(m?7:4),x=initialX+v.x*gather+chaos,y=initialY+v.y*gather+Math.sin((p+i)*3)*accumulate*1.6,rot=v.r*gather+(i%2?1:-1)*accumulate*2,sc=mix(1,v.s,gather);el.style.transform=`translate3d(${x}vw,${y}vh,0) rotate(${rot}deg) scale(${sc})`;el.style.opacity=String((1-fade*.94)*mix(.58,1,1-arrive));el.style.filter=`blur(${mix(0,3.6,fade)}px)`});const aOut=ease(range(p,.28,.53));if(copyA){copyA.style.opacity=String(1-aOut);copyA.style.transform=`translate3d(0,${-35*aOut}px,0)`;copyA.style.filter=`blur(${aOut*3}px)`}const bIn=ease(range(p,.42,.62)),bOut=ease(range(p,.84,.98));if(copyB){copyB.style.opacity=String(bIn*(1-bOut));copyB.style.transform=`translate3d(0,${mix(30,0,bIn)-20*bOut}px,0)`;copyB.style.filter=`blur(${(1-bIn)*2+bOut*3}px)`}}
function casesDesktop(){if(!cases||!track||reduced||isMobile())return;const p=prog(cases),max=Math.max(0,track.scrollWidth-innerWidth);track.style.transform=`translate3d(${-max*p}px,0,0)`;const exact=p*(caseEls.length-1),active=Math.round(exact);caseEls.forEach((el,i)=>{const d=Math.abs(i-exact),focus=clamp(1-d);el.style.opacity=String(mix(.25,1,focus));el.style.filter=`blur(${mix(5,0,focus)}px)`;el.style.transform=`scale(${mix(.92,1,focus)})`});if(caseFill)caseFill.style.width=(p*100)+'%';if(caseNum)caseNum.textContent=String(clamp(active+1,1,4)).padStart(2,'0')}
function casesMobile(){if(!isMobile()||reduced)return;caseEls.forEach((el,i)=>{const r=el.getBoundingClientRect(),p=clamp((innerHeight-r.top)/(innerHeight+r.height*.6)),art=$('.case-art',el),copy=$('.case-copy',el),enter=ease(range(p,.05,.42)),settle=ease(range(p,.32,.67));if(copy){copy.style.opacity=String(enter);copy.style.transform=`translate3d(0,${mix(34,0,enter)}px,0)`}if(art){const dir=i%2?1:-1;art.style.opacity=String(ease(range(p,.16,.55)));art.style.transform=`translate3d(${mix(dir*10,0,settle)}vw,${mix(30,0,settle)}px,0) scale(${mix(.84,.92,settle)})`}if(i===0){const s3=$('.s3',el);if(s3)s3.style.transform=`translate(-20%,-50%) rotate(${mix(13,7,settle)}deg) translateX(${mix(35,0,settle)}px)`}if(i===1){const fp=$('.field-phone',el);if(fp)fp.style.transform=`translateY(-50%) rotate(${mix(13,7,settle)}deg) translateX(${mix(28,0,settle)}px)`}if(i===2){const hb=$('.human-bridge',el);if(hb)hb.style.opacity=String(mix(.25,1,enter))}if(i===3){const prep=$('.prepared',el);if(prep)prep.style.transform=`translateY(-50%) translateX(${mix(35,0,settle)}px)`}})}
function pivotMotion(){if(!pivot||reduced)return;const p=prog(pivot),t=ease(range(p,.12,.68)),core=ease(range(p,.43,.78)),m=isMobile(),vec=m?[[-14,-12],[15,-13],[-15,13],[14,12],[0,-18],[4,18]]:[[-30,-24],[32,-24],[-34,25],[31,24],[1,-35],[8,34]];sels.forEach((el,i)=>{const[x,y]=vec[i];el.style.transform=`translate3d(${x*t}vw,${y*t}vh,0) scale(${mix(1,.72,t)})`;el.style.opacity=String(1-t*.8)});if(selCore){const base=m?'translate(50%,-50%)':'translateY(-50%)';selCore.style.opacity=String(core);selCore.style.transform=`${base} scale(${mix(.82,1,core)})`}}
function buildMotion(){if(!build||reduced)return;const p=prog(build),ingest=ease(range(p,.09,.55)),settle=ease(range(p,.39,.70)),out=ease(range(p,.63,.88)),m=isMobile(),vec=m?[[23,7],[23,-2],[17,-11]]:[[31,11],[29,-8],[24,-24]];inputs.forEach((el,i)=>{const[x,y]=vec[i];el.style.transform=`translate3d(${x*ingest}vw,${y*ingest}vh,0) scale(${mix(1,.65,ingest)})`;el.style.opacity=String(1-ingest*.94);el.style.filter=`blur(${ingest*2}px)`});if(product)product.style.transform=`translateY(-50%) scale(${mix(.95,1,settle)})`;if(output){output.style.opacity=String(out);output.style.transform=`translate3d(0,${mix(28,0,out)}px,0) rotate(${mix(4,0,out)}deg)`}}
function evolveMotion(){if(!evolve||reduced)return;const r=evolve.getBoundingClientRect(),p=clamp((innerHeight-r.top)/(innerHeight+r.height*.45)),t=ease(range(p,.40,.70));newEls.forEach(el=>{el.style.opacity=String(t);el.style.transform=`translateY(${mix(10,0,t)}px)`});if(need){const n=ease(range(p,.52,.80));need.style.opacity=String(n);need.style.transform=`translateY(${mix(24,0,n)}px)`}}
function globalUI(){const max=Math.max(1,document.documentElement.scrollHeight-innerHeight),p=clamp(scrollY/max);if(bar)bar.style.width=(p*100)+'%';if(head)head.classList.toggle('scrolled',scrollY>22)}
let ticking=false;function update(){ticking=false;globalUI();heroMotion();isMobile()?casesMobile():casesDesktop();pivotMotion();buildMotion();evolveMotion()}addEventListener('scroll',()=>{if(!ticking){ticking=true;requestAnimationFrame(update)}},{passive:true});addEventListener('resize',update,{passive:true});update();
})();
