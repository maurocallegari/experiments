(()=>{'use strict';const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));const progress=document.createElement('div');progress.className='progress';document.body.appendChild(progress);const reveals=[...document.querySelectorAll('.reveal')];if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.15});reveals.forEach(x=>io.observe(x))}else reveals.forEach(x=>x.classList.add('in'));let ticking=false;function update(){ticking=false;const max=document.documentElement.scrollHeight-innerHeight;const p=max?scrollY/max:0;progress.style.width=(p*100)+'%';document.documentElement.style.setProperty('--page',p);const desk=document.querySelector('.desk');if(desk){const hp=clamp(scrollY/innerHeight,0,1);desk.querySelectorAll('[data-x]').forEach((el,i)=>{const x=Number(el.dataset.x||0)*hp,y=Number(el.dataset.y||0)*hp,r=Number(el.dataset.r||0)*hp;el.style.translate=`${x}px ${y}px`;el.style.rotate=`${r}deg`})}const strike=document.querySelector('.strike');if(strike){const rect=strike.closest('.hero').getBoundingClientRect();strike.style.setProperty('--strike',clamp(-rect.top/(innerHeight*.65),0,1))}const steps=document.querySelector('.steps');if(steps){const r=steps.getBoundingClientRect(),travel=steps.offsetHeight-innerHeight,p2=clamp(-r.top/Math.max(1,travel),0,1),words=[...steps.querySelectorAll('.stepword')],idx=Math.min(words.length-1,Math.floor(p2*words.length));words.forEach((w,i)=>w.classList.toggle('active',i===idx))}const sh=document.querySelector('.shredder');if(sh){const r=sh.closest('.hero').getBoundingClientRect(),p3=clamp(-r.top/(innerHeight*.75),0,1);document.querySelectorAll('.shreds i').forEach((x,i)=>{x.style.setProperty('--drop',(p3*(55+(i%4)*18))+'px');x.style.setProperty('--rot',((i%2?1:-1)*p3*(3+i%3))+'deg')})}const cinema=document.querySelector('.cinema .scrollspace');if(cinema){const r=cinema.getBoundingClientRect(),travel=cinema.offsetHeight-innerHeight,p4=clamp(-r.top/Math.max(1,travel),0,1),world=cinema.querySelector('.world'),end=cinema.querySelector('.endcopy');world.style.setProperty('--zoom',1+p4*5.5);end.style.setProperty('--end',clamp((p4-.7)/.2,0,1))}const hot=document.querySelector('.lineflow path.hot');if(hot){const c=hot.closest('.canvas').getBoundingClientRect(),p5=clamp((innerHeight*.8-c.top)/(innerHeight*.8),0,1);hot.style.strokeDasharray='1';hot.style.pathLength='1';hot.style.strokeDashoffset=1-p5}}function req(){if(!ticking){ticking=true;requestAnimationFrame(update)}}addEventListener('scroll',req,{passive:true});addEventListener('resize',req,{passive:true});req();const workspace=document.querySelector('.os .workspace'),cursor=document.querySelector('.os .cursor');if(workspace&&cursor&&!matchMedia('(pointer:coarse)').matches){workspace.addEventListener('pointermove',e=>{const r=workspace.getBoundingClientRect();cursor.style.left=(e.clientX-r.left-11)+'px';cursor.style.top=(e.clientY-r.top-11)+'px'})}})();