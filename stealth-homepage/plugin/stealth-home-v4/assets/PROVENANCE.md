# Asset origin

request.svg, quote.svg, sheet.svg and report.svg are original vector UI components authored for this plugin on 2026-09-05. Each contains an embedded metadata element. PNG copies are deterministic Sharp rasterizations from the matching SVG. Example names/forms are synthetic; no client documents are included. The geometry represents documents and interface components, not photographs or traced artwork. No stock or external image licence is required.

WordPress media IDs: request 963; quote 964; sheet 965; report 966. Host binding in media.json prevents reusing those IDs on another installation. If an attachment is removed, the plugin uses its bundled SVG.
